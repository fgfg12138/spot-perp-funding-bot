## 2026-07-01 全局代码检查

- 完成全局 check：架构师审查 + QA 全量验证
- 质量评分 7.2/10（上一轮约 6.5/10 → 修复后仍约 7.2/10，因为本轮审查更严格且新增 Info 级别）
- 问题分布：P0=4 / P1=12 / P2=14 / P3=8，合计 38（上一轮 36）
- 关键改善：Critical -5（空 catch 已治理）、Major -3（门禁改为能力检测）
- 关键短板：生产代码仍有 110 处 `as any`、87 处 `process.env` 直接访问、15 个超 300 行文件
- 测试：738 passed / 67 files / 11.18s；`tsconfig.ci.json` 类型检查 0 错误；i18n lint 通过
- 报告落盘：`docs/reviews/global-check-2026-07-01.md`、`docs/reviews/global-check-test-report-2026-07-01.md`

## 2026-07-01 P2 超长函数拆分 + autoTransfer 回归修复

- 完成用户选择的 C 选项：拆分超长函数 + 补测试
- 修复 `autoTransferExecutor.ts` 拆分引入的回归 bug（`confirmAfterBalancesAndDelta` delta 方向判断）
- 拆分 `workerAutoExecution.ts` 4 个超长函数：
  - `tryAutoEntry` → 5 个子函数
  - `executeOrderPlan` → 3 个子函数
  - `tryAutoMonitor` → 3 个子函数
  - `tryExecuteClose` → 4 个子函数
- 新建 `workerExecutionHelpers.ts` 集中纯函数 helper
- 新建 `workerAutoExecution.test.ts`（52 个用例）
- 扩展 `autoTransferExecutor.test.ts`  helper 单测（新增 27 个用例）
- 测试结果：817 passed / 68 files / 6.45s；`tsconfig.ci.json` 0 错误
- 全局一致性审查：IS_PASS: YES
- 设计文档：`docs/architecture/refactor-longfuncs-2026-07-01.md`
- 工程师 agent 因 OpenCode Go 5 小时额度到上限曾失败，后用 `model: "lite"` 重跑成功

## 2026-07-01 as any / process.env 全量清洗（8 阶段完成）

### 方案设计
- 架构师设计清洗方案：`docs/architecture/as-any-process-env-cleanup-2026-07-01.md`
- 分 8 阶段执行：P0, 阶段1–8

### P0 静默失败修复
- `sqliteRepository.ts` — 所有空 catch 块增加 `console.error`
- `fileSystemRepository.ts` — queryAll 增加脏行保护、deleteById 加日志
- 新增局部 `formatErr()` 辅助函数

### 阶段1：RuntimeConfig 统一入口
- 新建 `lib/strategy-v121/config/runtimeConfig.ts`
- 接口：`RuntimeConfig`、`getRuntimeConfig()`、`loadRuntimeConfig()`、`resetRuntimeConfig()`
- 包括所有 V121_* 环境变量便捷 getter
- 配套测试 `runtimeConfig.test.ts`

### 阶段2：替换 process.env 直接访问
- 涉及文件：runtimeAdapterFactory、binance/okxAccountAdapter、guardedOrderExecutor、guardedCloseExecutor、closePrecheckGate、capitalPrecheck、orderIntent
- 所有测试改为 `resetRuntimeConfig({...})` 注入 mock

### 阶段3：类型化 persistence 行
- 新建 `lib/strategy-v121/persistence/repositoryRowTypes.ts`
- 定义 LatestScanRow、WorkerHeartbeatRow 等 5 个 row 接口
- 提供 readTimestamp/readString/readNumber/readBoolean/readJson 辅助函数
- 修改 9 个 ledger/store 文件消除 `as any[]`
- 配套测试 `repositoryRowTypes.test.ts`

### 阶段4：Adapter 私有字段 + ExchangeError
- 新建 `lib/strategy-v121/account/adapters/exchangeError.ts`
- runtimeAdapterFactory binance/okx adapter 密钥存储改为 `#private` 字段
- 替换所有 `(err as any).code` 为 ExchangeError 工具函数
- 配套测试 `exchangeError.test.ts`

### 阶段5：Worker 层市场快照类型化
- `workerAutoExecution.ts` + `workerExecutionHelpers.ts` — 替换 17 处 `as any`/`as unknown[]`
- 新增 `tickerToMarketSnapshot` 辅助函数

### 阶段6：枚举/状态类型守卫
- 新增 `isExchangeId`、`isOrderIntentPurpose`、`isOrderExecutionStatus`、`isPersistenceMode`
- 替换 `finalPreExecutionAudit`、`capitalPlanner`、`repositoryFactory` 中的 `as any` 断言

### 阶段7：剩余 process.env 收口
- 搜索 lib/strategy-v121/ 下所有剩余 `process.env` 直接访问并收口到 runtimeConfig
- 修复 `workerAutoExecution.test.ts` 遗留兼容问题（env 参数 -> resetRuntimeConfig）

### 阶段8：CI 规则 + 测试补强
- 新建 `eslint.config.mjs` — 对 lib/strategy-v121/ 启用 `@typescript-eslint/no-explicit-any: error`（捕获 284 处 pre-existing as any）
- 测试补强：runtimeConfig +12 用例、exchangeError +6 用例、repositoryRowTypes +6 用例、新增 types.test.ts（8 用例）

### 最终结果
- 测试：883 passed / 72 files（从 532 增长到 883）
- TypeScript：仅 2 个 pre-existing 错误（accountSigning.ts，非清洗范围）
- ESLint：284 as any 被规则捕获并接受为 pre-existing
- 全局一致性审查：IS_PASS: YES

